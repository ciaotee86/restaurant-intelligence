services:
  - type: web
    name: restaurant-intelligence
    env: docker
    dockerfilePath: Dockerfile
    plan: free
    region: singapore
    envVars:
      - key: GEMINI_API_KEY
        sync: false
      - key: PORT
        value: 10000
